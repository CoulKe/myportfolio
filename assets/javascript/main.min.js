const summary=document.querySelectorAll(".summary");let full_desc=document.querySelectorAll(".full-description");summary.forEach(function(s,index){s.addEventListener("click",function(){full_desc[index].style.display="block"})});